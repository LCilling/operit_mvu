{
  "name": "operit_mvu",
  "version": "3.0.0",
  "private": true,
  "description": "Dynamic character state ToolPkg for OperitAI",
  "license": "MIT",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "audit": "node scripts/audit-manifest.mjs && node scripts/audit-host-api-compat.mjs && node scripts/audit-web-ui.mjs && node scripts/audit-v3-ui.mjs",
    "audit:manifest": "node scripts/audit-manifest.mjs",
    "audit:host": "node scripts/audit-host-api-compat.mjs",
    "audit:package": "node scripts/audit-v3-package.mjs",
    "audit:v3": "node scripts/audit-v3-ui.mjs",
    "audit:effects": "node scripts/audit-temporary-effects.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "build:web": "node scripts/build-web.mjs",
    "build": "pnpm run audit && pnpm run typecheck && pnpm run audit:effects && pnpm run build:web && pnpm run audit:host",
    "check": "pnpm run build",
    "pack": "pnpm run build && node scripts/pack.mjs && pnpm run audit:package"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
